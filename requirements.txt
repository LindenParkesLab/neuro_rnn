# Exact dependency pins for the neuro_rnn analyses.
#
# Single source of truth: environment.yml installs Python, then defers to this
# file. Every package here is directly imported by src/ or scripts/ -- see the
# README for what each is used for.
#
# Note on version coupling: neurogym 2.1.0 hard-pins numpy==2.1.*, scipy==1.14.*
# and matplotlib==3.9.*. The pins below sit inside those ranges, so do not bump
# numpy, scipy or matplotlib independently of neurogym.
# gymnasium (==0.29.*) is installed transitively by neurogym.

# --- core numerics ---
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3

# --- modelling / training ---
torch==2.7.1
neurogym==2.1.0

# Required by neurogym 2.1.0 at import time, despite being declared only under
# its optional "rl" extra: neurogym/utils/plotting.py imports sb3_contrib at
# module level, so `import neurogym` fails with ModuleNotFoundError without
# these. Our code never imports them directly. Do not remove.
stable-baselines3==2.6.0
sb3-contrib==2.6.0

# --- analysis ---
scikit-learn==1.6.1       # PCA of hidden-state dynamics
statsmodels==0.14.5       # linear mixed-effects for the geometry null
bctpy==0.6.1              # graph metrics (rich-club, clustering, participation)
joblib==1.4.2             # parallelism for null distributions

# --- neuroimaging I/O ---
nibabel==5.3.2
nilearn==0.12.0

# --- data I/O ---
h5py==3.14.0              # trained model / output storage
PyYAML==6.0.2             # paths.yaml config

# --- figures & notebooks ---
matplotlib==3.9.4
seaborn==0.13.2
tqdm==4.67.1
jupyterlab==4.4.4
